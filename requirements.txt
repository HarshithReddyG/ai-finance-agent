# --- Core (Phase 1-3) ---
pandas==2.2.2
python-dotenv>=1.0.1
SQLAlchemy>=2.0
pytest>=8.0

# --- Mock data generation (Phase 2) ---
Faker>=25.0

# --- Analytics / anomaly detection (Phase 5-6) ---
numpy>=1.26
scipy>=1.13

# --- Interface (Phase 8) ---
typer>=0.12
streamlit>=1.34

# --- LLM / agent tooling (Phase 4, 7) ---
# ollama python client is optional — only needed if LLM_PROVIDER=ollama
ollama>=0.2
anthropic>=0.30
